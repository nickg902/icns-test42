{
  "name": "int13h.github.io",
  "version": "1.0.0",
  "description": "",
  "repository": {
    "type": "git",
    "url": "https://github.com/int13h.github.io"
  },
  "author": "int13h",
  "license": "UNLICENSED",
  "scripts": {
    "build-dev": "esbuild index.jsx --bundle --define:process.env.NODE_ENV=\\\"development\\\" --sourcemap --outfile=../index.js",
    "build-prod": "esbuild index.jsx --bundle --define:process.env.NODE_ENV=\\\"production\\\" --minify --outfile=../index.js"
  },
  "browser": {
    "path": "path-browserify"
  },
  "dependencies": {
    "@material-ui/core": "^4.11.1",
    "esbuild": "^0.8.16",
    "react": "^17.0.1",
    "react-router-dom": "^5.2.0"
  }
}
